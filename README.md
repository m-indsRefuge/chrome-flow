# Constellation

Constellation is a local-first browser cognitive workspace for organising live research, durable workspace memory, recovery, and future deterministic, algorithmic, and AI-assisted workflows.

Former project name: **Chrome Flow**.

## Current product direction

Constellation is being built as a Chrome extension that helps the Operator:

- create and maintain browser workspaces;
- assign aliases and research roles to workspace tabs;
- organise tabs into native Chrome groups;
- preserve User Journal, System Journal, and Recovery Journal records separately;
- save exact workspace snapshots into the Workspace Library;
- resume paused or archived workspaces through checked transactional actions;
- maintain browser projection metadata automatically;
- support several simultaneous live workspaces through the forthcoming multi-window runtime and window-binding foundation.

The deterministic system owns workspace identity, browser projection, persistence, recovery, permissions, and recall. Future algorithms may infer relationships, and future AI integrations may explain patterns, but neither layer may silently replace deterministic authority or Operator control.

## Current development install

Repository:

```text
https://github.com/m-indsRefuge/constellation.git
```

Canonical local development path:

```text
C:\Users\nolan\AIProjects\constellation
```

Active unpacked extension ID:

```text
hkakifedpohjilmjiiobcmmgemgighli
```

Development steps:

1. Open Chrome.
2. Go to `chrome://extensions`.
3. Enable Chrome Extensions Developer mode.
4. Click **Load unpacked**.
5. Select `C:\Users\nolan\AIProjects\constellation`.
6. Click the **Constellation** extension action.
7. The side panel should open.

The former Chrome Flow installation and local path have completed their controlled retirement. The retired repository is preserved as historical rollback evidence and must not be used as the active development workspace.

## Current engineering phase

```text
Layer 2.3B — Concurrency Characterization and Engineering Foundation
```

The accepted Layer 2 runtime remains a validated single-window foundation. Before concurrent multi-window implementation, this phase establishes runtime ownership documentation, pure characterization tests, repeatable validation commands, and the contracts required for safe identity and mutation coordination.

## Agent and contributor instructions

Before changing the repository, read:

- [`AGENTS.md`](AGENTS.md)
- [`Constellation Authority Contract`](docs/architecture/CONSTELLATION-AUTHORITY-CONTRACT.md)
- [`Protected Identities and Compatibility Register`](docs/architecture/PROTECTED-IDENTITIES.md)
- [`Validation Protocol`](docs/development/VALIDATION-PROTOCOL.md)
- [`Definition of Done`](docs/development/DEFINITION-OF-DONE.md)
- [`Codex Workflow`](docs/development/CODEX-WORKFLOW.md)

These documents protect deterministic authority, compatibility, validation evidence, and Operator control.

## Build principle

Constellation Core owns workspace state.

The product must remain useful with no AI provider configured. AI providers may later assist with summaries, relationship explanations, pattern recognition, and workflow recommendations only above the deterministic and algorithmic foundations.
